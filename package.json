{
  "name": "next-clipboard-text-sync",
  "private": true,
  "scripts": {
    "dev": "pnpm -F node-api dev & pnpm -F front dev",
    "dev:api": "pnpm -F node-api dev",
    "dev:front": "pnpm -F front dev",
    "prisma:generate": "pnpm -F node-api prisma generate"
  },
  "dependencies": {
    "eslint": "^9.39.1",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.10"
  }
}